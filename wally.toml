[package]
name = "vivuyr/networker"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

license = "MIT"
authors = ["vivuyr"]

description = "A lightweight networking library for Roblox."

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml", "LICENSE"]
